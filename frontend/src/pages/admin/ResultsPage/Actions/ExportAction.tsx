import React from 'react'
import Button from 'react-bootstrap/Button'

const ExportAction: React.FunctionComponent = () => {
  return <Button variant="light">Export to CSV</Button>
}

export default ExportAction
